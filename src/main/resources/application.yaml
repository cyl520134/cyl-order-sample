
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/wb?useUnicode=true&characterEncoding=utf-8&serverTimeZone=UTC
    username: root
    password: cbfgmz819

server:
  port: 9090

mybatis:
  mapper-locations: classpath:mapper/*.xml